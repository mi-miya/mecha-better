<% if logged_in? %>
  <%= link_to 'log out', '/logout' %>
  <%= @user.name %>さん、こんにちは。
  <%= image_tag(@user.icon) %>
<% else %>
  <%= link_to("SignIn or SignUp", signin_path) %>
<% end %>

<ul>
    <li>サイトの簡単な説明</li>
    <li>新着Idea</li>
    <li>人気Idea</li>
</ul>